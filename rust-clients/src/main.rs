mod core; 
mod init; 
mod daemon;  

#[tokio::main]
async fn main() {
    println!("--"); 
    //
    // let (event_transaction, daemon_handle) = spawn_daemon(runner, &clients, &edges); 
    // let _ = daemon_handle.await; 
}
