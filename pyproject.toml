[build-system]
requires = ["setuptools>68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "topographic-models"
version = "0.0.1"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools]
packages = ["models"]
